apiVersion: v1
kind: Service
metadata:
  name: {{ include "rumi-reporting.fullname" . }}
  labels:
    {{- include "rumi-reporting.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rumi-reporting.selectorLabels" . | nindent 4 }}
