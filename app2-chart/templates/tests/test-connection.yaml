apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rumi-reporting.fullname" . }}-test-connection"
  labels:
    {{- include "rumi-reporting.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rumi-reporting.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
